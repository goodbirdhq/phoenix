# Ping

Post `{"ok": true, "echo": <input>}` to the callback, where `<input>` is the
input you were given verbatim. The run instructions you received carry the
exact callback protocol (URL, auth, payload shape) — this skill only defines
what to report.
