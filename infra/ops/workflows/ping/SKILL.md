# Ping

Post `{"ok": true, "echo": <input>}` to the callback, where `<input>` is the input you were given verbatim. Follow the run instructions appended by the ops hub for the exact callback protocol (URL, auth, payload shape) — this skill only defines what to report.
